# Microbenchmarks
Generated by `scripts/rt-microbench4`; raw values in `micro.json`.

## M6 — scheduler pass cost on four workers (light mix 1.25/1.25/2.5/5 Msps x 2.090, N = 1024, rotation 1, 5.0 s, input: pacer, trace mask 0x17f, 4194304 records per ring, selection pass bounded at 24 µs)

Calibration (rm at ratio 4096, 4.0 s steps, band 0.93-0.95 for the busiest worker's demand): x1.000 -> 0.389; x2.414 -> 0.909 (saturated); x1.707 -> 0.768; x2.090 -> 0.818. Scale used: 2.0903394266908095 (band not reached: the largest scale that kept up; the reference saturated at 2.41351504794372)

In the workload's units, the same under every policy (docs/m6-runbook.md 9): the busiest worker's demand, productive work() calls per second per worker, the end-to-end latency from the pacer's write to the sink (mean / p99 µs, fastest receiver), the entry block's wait from write to first run (mean / p99 µs, fastest receiver), and whether the run kept up:

| policy | ratio | demand max | productive calls/s per worker | E2E rx3 mean / p99 | entry wait rx3 mean / p99 | saturated |
|---|---|---|---|---|---|---|
| rr | 16 | 0.823 | 77171, 80290, 80134, 81405 | 2888.3 / 8539.4 | 265.6 / 2022.1 | yes |
| edf | 16 | 0.811 | 78017, 80966, 83405, 82646 | 464.0 / 1468.7 | 16.6 / 81.3 | no |
| rm | 16 | 0.807 | 77158, 80662, 82114, 81570 | 397.7 / 586.6 | 10.3 / 71.5 | no |
| rr | 4096 | 0.808 | 77648, 80832, 80669, 81769 | 581.3 / 852.8 | 15.0 / 100.1 | no |
| edf | 4096 | 0.808 | 77987, 80973, 83447, 82639 | 409.6 / 582.1 | 10.0 / 67.5 | no |
| rm | 4096 | 0.806 | 77236, 80669, 82083, 81469 | 398.3 / 661.2 | 9.6 / 66.2 | yes |

Batch response time per receiver (mean µs, and the fraction of batches later than the receiver's own period):

| policy | ratio | rx 0 | rx 1 | rx 2 | rx 3 (5 Msps) | EDF pipeline misses |
|---|---|---|---|---|---|---|
| rr | 16 | 276 (11.7%) | 294 (13.7%) | 319 (93.0%) | 1741 (100.0%) | - |
| edf | 16 | 623 (89.5%) | 842 (99.0%) | 448 (98.8%) | 230 (99.2%) | 5908 |
| rm | 16 | 456 (62.9%) | 855 (98.9%) | 348 (96.1%) | 160 (94.9%) | - |
| rr | 4096 | 223 (0.9%) | 248 (1.6%) | 278 (86.4%) | 309 (100.0%) | - |
| edf | 4096 | 515 (77.9%) | 687 (95.8%) | 364 (96.8%) | 173 (98.3%) | 152 |
| rm | 4096 | 445 (58.8%) | 829 (98.9%) | 346 (96.3%) | 159 (94.6%) | - |

Scheduler events per worker inside the window (rate, mean and p99 duration, share of the worker's time):

| policy | ratio | event | per s | mean µs | p99 µs | busy % |
|---|---|---|---|---|---|---|
| rr | 16 | sweep | 67515 | 14.3 | 122.9 | 96.7 |
| rr | 16 | messagePhase | 4220 | 6.9 | 17.1 | 2.9 |
| rr | 16 | stateSync | 4220 | 4.6 | 12.8 | 1.9 |
| rr | 16 | schedulerMessages | 736 | 0.1 | 0.5 | 0.0 |
| rr | 16 | removalCleanup | 4220 | 0.0 | 0.1 | 0.0 |
| rr | 16 | blockMessages | 4220 | 0.3 | 0.8 | 0.1 |
| rr | 16 | workProbe | 937388 | 0.0 | 0.0 | 0.0 |
| edf | 16 | sweep | 213584 | 4.2 | 55.6 | 90.3 |
| edf | 16 | messagePhase | 13349 | 6.2 | 14.1 | 8.3 |
| edf | 16 | stateSync | 13349 | 4.7 | 11.0 | 6.3 |
| edf | 16 | schedulerMessages | 2518 | 0.1 | 0.2 | 0.0 |
| edf | 16 | removalCleanup | 13349 | 0.0 | 0.1 | 0.0 |
| edf | 16 | blockMessages | 13349 | 0.2 | 0.6 | 0.2 |
| edf | 16 | releaseScan | 295671 | 0.5 | 1.2 | 15.1 |
| edf | 16 | workProbe | 827 | 0.0 | 0.0 | 0.0 |
| rm | 16 | sweep | 56870 | 17.1 | 346.0 | 97.5 |
| rm | 16 | messagePhase | 3555 | 6.2 | 14.9 | 2.2 |
| rm | 16 | stateSync | 3555 | 4.2 | 12.2 | 1.5 |
| rm | 16 | schedulerMessages | 685 | 0.1 | 0.2 | 0.0 |
| rm | 16 | removalCleanup | 3555 | 0.0 | 0.1 | 0.0 |
| rm | 16 | blockMessages | 3555 | 0.3 | 0.6 | 0.1 |
| rm | 16 | workProbe | 852935 | 0.0 | 0.0 | 0.0 |
| rr | 4096 | sweep | 89605 | 11.1 | 105.1 | 99.5 |
| rr | 4096 | messagePhase | 21 | 21.6 | 29.1 | 0.0 |
| rr | 4096 | stateSync | 21 | 14.2 | 20.3 | 0.0 |
| rr | 4096 | schedulerMessages | 4 | 0.7 | 1.1 | 0.0 |
| rr | 4096 | removalCleanup | 21 | 0.0 | 0.1 | 0.0 |
| rr | 4096 | blockMessages | 21 | 1.0 | 1.4 | 0.0 |
| rr | 4096 | workProbe | 1268359 | 0.0 | 0.0 | 0.0 |
| edf | 4096 | sweep | 320235 | 3.1 | 43.5 | 98.0 |
| edf | 4096 | messagePhase | 78 | 13.2 | 21.8 | 0.1 |
| edf | 4096 | stateSync | 78 | 9.0 | 15.2 | 0.1 |
| edf | 4096 | schedulerMessages | 16 | 0.4 | 0.8 | 0.0 |
| edf | 4096 | removalCleanup | 78 | 0.0 | 0.1 | 0.0 |
| edf | 4096 | blockMessages | 78 | 0.8 | 1.1 | 0.0 |
| edf | 4096 | releaseScan | 401496 | 0.5 | 1.1 | 22.1 |
| rm | 4096 | sweep | 63496 | 15.7 | 318.8 | 99.5 |
| rm | 4096 | messagePhase | 16 | 29.0 | 49.0 | 0.0 |
| rm | 4096 | stateSync | 16 | 17.7 | 28.9 | 0.0 |
| rm | 4096 | schedulerMessages | 3 | 1.0 | 1.5 | 0.0 |
| rm | 4096 | removalCleanup | 16 | 0.0 | 0.1 | 0.0 |
| rm | 4096 | blockMessages | 16 | 1.0 | 2.7 | 0.0 |
| rm | 4096 | workProbe | 952325 | 0.0 | 0.0 | 0.0 |

Jobs discarded by the re-sync (EDF only tracks jobs; mean per re-sync, max, share of re-syncs that discarded any): rr r16: 0.00, 0, 0%; edf r16: 0.00, 0, 0%; rm r16: 0.00, 0, 0%; rr r4096: 0.00, 0, 0%; edf r4096: 0.00, 0, 0%; rm r4096: 0.00, 0, 0%

Mean gap between consecutive passes of a worker (µs, one value per worker): rr r16: 0.5, 0.5, 0.5, 0.5; edf r16: 0.5, 0.4, 0.5, 0.5; rm r16: 0.5, 0.4, 0.5, 0.4; rr r4096: 0.1, 0.1, 0.1, 0.1; edf r4096: 0.1, 0.1, 0.1, 0.1; rm r4096: 0.1, 0.1, 0.1, 0.1

Passes ended by a bound (per s per worker, time budget / selection count; round robin has no bound): rr r16: 0.0 / 0.0; edf r16: 10597.9 / 0.0; rm r16: 0.0 / 67.2; rr r4096: 0.0 / 0.0; edf r4096: 9438.8 / 0.0; rm r4096: 0.0 / 68.3

### What the ratio is

`process_stream_to_message_ratio` is a setting of GR4's scheduler (`core/include/gnuradio-4.0/Scheduler.hpp`,
default 16). Every worker runs a loop of *passes*; in a pass it selects blocks and calls their `work()`. The
worker keeps a pass counter, and once every `ratio` passes it opens a *message phase* before doing any work.
The message phase is where everything that is not stream processing happens, in this order: the scheduler's and
graph's own messages (worker 0 only), cleanup of removed blocks, zombie reaping, adoption of new blocks, the
re-sync of every block's scheduling state (`stateSync`), the static re-ordering of the worker's block list,
each block's `processScheduledMessages()` (settings changes and other block messages), and buffer house-keeping
unless the house-keeping policy is Light. Worker 0 also opens the phase early when a scheduler message is
pending, so the ratio does not bound the latency of scheduler and graph messages; it does bound everything
else on that list. The ratio is a count of passes, not a time: the interval between two message phases is
`ratio` × the worker's pass duration, and the pass duration depends on the policy and the load (the table
below gives both for this run). A power of two is slightly cheaper (the counter wraps with a mask instead of
a modulo).

The re-sync is what makes the ratio matter to EDF. `syncSchedStates()` assigns whole scheduling states, and
under a policy that tracks releases the worker then rebuilds its release storage — on every message phase.
Until 2026-09-22 (`9126f2a`, `7af5453`) that rebuild flattened the whole graph and recomputed its adjacency
each time, and discarded every outstanding job and last-release time, whether or not the graph had changed.
Now the flattened graph and adjacency are cached per worker and rebuilt only when the scheduler's topology
generation has moved (it is bumped whenever work quiescence is released, which every structural change
takes), and the admitted jobs, last-release times and overrun counts are carried across a re-sync whose
block list is pointer-identical and whose generation is unchanged. The re-sync still runs on every message
phase; it is cheaper when nothing changed. Under RR and RM the re-sync is a few microseconds; under EDF it
is the storage rebuild. At the default ratio of 16 that recurs every 16 passes, which is the `stateSync` row
above (since `9126f2a` the marker covers the state rebuild alone; before, it ran to the end of the phase
and read as nearly all of it). At 4096 the same work recurs 256× less often and its busy share falls below
one percent, which is why the sweeps compare policies at 4096.

### Choosing and setting a value

The ratio trades scheduler overhead against reaction time. A larger ratio makes every message phase rarer:
less re-sync cost, more passes per second, but a longer wait before a settings change is applied, a new block
is adopted or a removed one is released. A smaller ratio does the reverse. The rule is: take the largest
ratio whose message-phase interval is still inside the latency the application can tolerate for a settings
change or a graph mutation, and check that `stateSync`'s busy share is negligible at that ratio. For a
static graph with no settings traffic while running, which is what the sweeps use, 4096 is that value on this
machine (an interval in the tens of milliseconds, re-sync below 1 %). A graph that expects settings messages
at a given rate needs an interval shorter than their spacing; divide that by the measured pass duration to
get the ratio, and round to a power of two. Do not pick the ratio to make one policy look better: it changes
the re-sync cost of EDF and the pass rate of every policy at once, so policies are compared at the same
ratio.

Setting it: `rx_latency4 --sched-ratio N` stages it as the scheduler setting `process_stream_to_message_ratio`
in the property map applied before the scheduler starts (staging then applying; `set()` alone changes
nothing); the sweep driver takes `sched_ratio` from the machine profile (`experiments/profiles/*.json`,
`x86-8core-r4096.json` is the 4096 variant) and passes it the same way; in GR4 code it is the constructor
setting `{"process_stream_to_message_ratio", gr::Size_t(N)}` of any `gr::scheduler::Simple`. The value each
run used is recorded as `sched_ratio` in its `latency_summary.json` and `trace_meta.json`.

| policy | ratio | passes per s | mean pass µs | message phases per s | interval between message phases ms | stateSync busy % |
|---|---|---|---|---|---|---|
| rr | 16 | 67515 | 14.3 | 4220 | 0.24 | 1.9 |
| edf | 16 | 213584 | 4.2 | 13349 | 0.07 | 6.3 |
| rm | 16 | 56870 | 17.1 | 3555 | 0.28 | 1.5 |
| rr | 4096 | 89605 | 11.1 | 21 | 47.47 | 0.0 |
| edf | 4096 | 320235 | 3.1 | 78 | 12.81 | 0.1 |
| rm | 4096 | 63496 | 15.7 | 16 | 64.01 | 0.0 |

## How this run reads

*Hand-written after the generated part; a regeneration of this file drops it.
Read per `docs/m6-runbook.md` §5–§9. Tree at `c65beed` (the analysis window
now ends where the input ends; the binaries are those of `a15966c`, nothing
compiled changed). Same KVM guest, RT kernel `7.0.11-rt`, CPUs 4–7 isolated,
workers pinned one per isolated CPU under `SCHED_OTHER`, the pacer on CPU 3.*

**What is different about this run, and why it is not comparable.** Three
things, on top of the pacer-mode differences of the previous run (§8 item 6):

1. **The calibration reference is rate monotonic at ratio 4096**, not round
   robin (`--m6-reference rm,4096`). Four 4 s steps: demand 0.389 at scale 1,
   2.414× saturated at 0.909, 1.707× at 0.768, 2.090× at 0.818 — kept up. The
   band was not reached; the runs use **2.090×** (2.61 / 2.61 / 5.23 /
   10.45 Msps; chunk periods 392 / 392 / 196 / 98 µs; pass budget 24 µs),
   90 % of the 2.331× the round-robin reference gave the previous run. RM
   saturates first not on the fastest receiver but by starving a slow one
   (chain 1), so this is the input at which the least capable policy on this
   workload just keeps up, and the busiest worker's demand is 0.81, not 0.92.
2. **The runs are 5 s, the calibration steps 4 s, and the rings 4 M records**
   (`0x17f` for the runs — `workPhases` dropped, which no M6 row reads —
   `0x104` for the calibration steps). The whole pass took 6 min 51 s against
   29 min for the published configuration. The price: the trace-based rows
   describe 0.4–0.6 s of each RR or RM run and 1.8–2.2 s of each EDF run
   (`window_s` per cell in `micro.json`), and a 5 s run is dominated by its
   start-up — the pacer's 0.5 s delay, the workers' first passes — so its
   saturation verdicts and late shares are less settled than a 20 s run's.
   The pacer-based figures (write lag, saturation, end-to-end latency) still
   cover the whole run.
3. **The window is clipped at the input's end** (`c65beed`), so no row here
   contains a receiver's post-input drain; the previous run's RM rows at
   ratio 16 did.

**At this input, EDF keeps up and both static policies fail somewhere.**
Read the "same under every policy" table first:

- **EDF** keeps up at both ratios: no retries on any receiver, write lag p99
  5–7 µs, demand 0.811 / 0.808. Its fastest receiver's end-to-end latency is
  464 µs mean / 1.47 ms p99 at ratio 16 and 410 / 582 µs at 4096, entry wait
  10–17 µs mean. Its slow receivers pay for that: 1.0–1.5 ms mean end-to-end,
  entry wait 55–76 µs. At 2.331× it saturated on the fastest receiver; at
  2.090× it does not.
- **Rate monotonic** saturates chain 1 at ratio 4096 (write lag p95 28 ms,
  26 023 retries, end-to-end 8.4 ms mean / 153 ms p99 — a starved receiver
  catching up in bursts) and is at the edge at ratio 16 (chain 1 write lag
  p99 4.6 ms, 4 722 retries, not saturated by the p95 rule). Its fastest
  receiver is the best served in the table at both ratios: 398 µs mean,
  587–661 µs p99, entry wait 10 µs. That is the reference's own edge, as the
  calibration intended.
- **Round robin** saturates its *fastest* receiver at ratio 16 — write lag
  p95 98 ms, 140 760 retries against 51 000 chunks (a chunk is retried until
  it fits: 2.8 retries per chunk on average), end-to-end
  2.9 ms mean / 8.5 ms p99, entry wait 266 µs mean — and keeps up at 4096
  (581 µs / 853 µs, entry wait 15 µs, no retries). At 2.331× and 20 s, round
  robin at ratio 16 kept up; whether this 5 s failure is the policy's
  start-up transient at the default ratio (a message phase every 16 passes
  of 14.8 µs) or a repeatable capacity difference between the two ratios is
  a question for a longer run, not this one.

Demand is 0.806–0.823 in every cell, within ±0.02 of the calibration's 0.818
(§6): every policy saw the same input, and the differences above are what
each did with it.

**Scheduler costs, in each policy's own units (§9).** Pass periods RR
14.8 / 11.2 µs (ratio 16 / 4096), EDF 4.7 / 3.1, RM 17.6 / 15.8; the gap
between passes 0.4–0.5 µs at 16 and 0.06–0.07 at 4096 on every worker.
`stateSync` 4.3–4.7 µs at ratio 16 under all three; EDF's message phases
take 8.3 % of a worker at ratio 16 (13 349 per second) and 0.1 % at 4096,
its release scan 15–22 %; its pass is ended by the 24 µs budget on 5 % of
passes at ratio 16 and 3 % at 4096. The re-sync discarded no jobs. These
rows rest on windows of 0.4–2.2 s and are read for direction, not for a
figure a longer run will reproduce.
