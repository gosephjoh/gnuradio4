# Microbenchmarks
Generated by `scripts/rt-microbench4`; raw values in `micro.json`.

## M6 — scheduler pass cost on four workers (light mix 1.25/1.25/2.5/5 Msps, N = 1024, rotation 1, 20 s, every category traced, selection pass bounded at 51 µs)

Batch response time per receiver (mean µs, and the fraction of batches later than the receiver's own period):

| policy | ratio | rx 0 | rx 1 | rx 2 | rx 3 (5 Msps) | EDF pipeline misses |
|---|---|---|---|---|---|---|
| rr | 16 | 134 (0.0%) | 153 (0.2%) | 160 (0.4%) | 169 (19.7%) | - |
| edf | 16 | 164 (0.0%) | 189 (0.0%) | 148 (0.3%) | 136 (4.5%) | 12 |
| rm | 16 | 158 (0.4%) | 216 (0.2%) | 150 (0.6%) | 119 (1.4%) | - |
| rr | 4096 | 184 (0.0%) | 138 (0.1%) | 153 (0.4%) | 166 (18.1%) | - |
| edf | 4096 | 157 (0.1%) | 222 (0.0%) | 145 (0.4%) | 133 (4.1%) | 5 |
| rm | 4096 | 157 (0.0%) | 158 (0.4%) | 145 (0.4%) | 109 (1.1%) | - |

Scheduler events per worker inside the window (rate, mean and p99 duration, share of the worker's time):

| policy | ratio | event | per s | mean µs | p99 µs | busy % |
|---|---|---|---|---|---|---|
| rr | 16 | sweep | 90214 | 9.9 | 70.5 | 89.0 |
| rr | 16 | messagePhase | 5638 | 7.3 | 17.5 | 4.1 |
| rr | 16 | stateSync | 5638 | 4.5 | 13.0 | 2.6 |
| rr | 16 | schedulerMessages | 1473 | 0.1 | 0.2 | 0.0 |
| rr | 16 | removalCleanup | 5638 | 0.7 | 0.9 | 0.4 |
| rr | 16 | blockMessages | 5638 | 0.3 | 0.5 | 0.2 |
| rr | 16 | workProbe | 1488327 | 0.0 | 0.0 | 0.0 |
| edf | 16 | sweep | 156967 | 4.5 | 40.6 | 71.3 |
| edf | 16 | messagePhase | 9811 | 13.8 | 25.3 | 13.6 |
| edf | 16 | stateSync | 9811 | 11.0 | 22.0 | 10.8 |
| edf | 16 | schedulerMessages | 3072 | 0.1 | 0.2 | 0.0 |
| edf | 16 | removalCleanup | 9811 | 0.9 | 1.2 | 0.9 |
| edf | 16 | blockMessages | 9810 | 0.2 | 0.5 | 0.2 |
| edf | 16 | releaseScan | 319769 | 0.7 | 1.5 | 21.6 |
| edf | 16 | workProbe | 118972 | 0.0 | 0.0 | 0.0 |
| rm | 16 | sweep | 78204 | 11.6 | 101.5 | 90.7 |
| rm | 16 | messagePhase | 4888 | 7.4 | 17.3 | 3.6 |
| rm | 16 | stateSync | 4888 | 4.7 | 13.5 | 2.3 |
| rm | 16 | schedulerMessages | 1336 | 0.1 | 0.2 | 0.0 |
| rm | 16 | removalCleanup | 4888 | 0.7 | 0.9 | 0.3 |
| rm | 16 | blockMessages | 4888 | 0.2 | 0.5 | 0.1 |
| rm | 16 | workProbe | 1321373 | 0.0 | 0.0 | 0.0 |
| rr | 4096 | sweep | 93124 | 9.8 | 65.3 | 91.4 |
| rr | 4096 | messagePhase | 23 | 34.4 | 55.8 | 0.1 |
| rr | 4096 | stateSync | 23 | 24.5 | 35.8 | 0.1 |
| rr | 4096 | schedulerMessages | 6 | 1.6 | 6.6 | 0.0 |
| rr | 4096 | removalCleanup | 23 | 0.9 | 1.4 | 0.0 |
| rr | 4096 | blockMessages | 23 | 1.8 | 7.1 | 0.0 |
| rr | 4096 | workProbe | 1539254 | 0.0 | 0.0 | 0.0 |
| edf | 4096 | sweep | 208933 | 3.9 | 36.5 | 81.0 |
| edf | 4096 | messagePhase | 51 | 28.0 | 48.9 | 0.1 |
| edf | 4096 | stateSync | 51 | 21.4 | 41.1 | 0.1 |
| edf | 4096 | schedulerMessages | 18 | 0.4 | 1.1 | 0.0 |
| edf | 4096 | removalCleanup | 51 | 0.9 | 1.2 | 0.0 |
| edf | 4096 | blockMessages | 51 | 1.1 | 2.7 | 0.0 |
| edf | 4096 | releaseScan | 398054 | 0.7 | 1.4 | 28.5 |
| edf | 4096 | workProbe | 145294 | 0.0 | 0.0 | 0.0 |
| rm | 4096 | sweep | 79815 | 11.5 | 101.4 | 92.1 |
| rm | 4096 | messagePhase | 19 | 30.7 | 44.8 | 0.1 |
| rm | 4096 | stateSync | 19 | 21.3 | 33.1 | 0.0 |
| rm | 4096 | schedulerMessages | 6 | 1.1 | 2.2 | 0.0 |
| rm | 4096 | removalCleanup | 19 | 1.0 | 1.5 | 0.0 |
| rm | 4096 | blockMessages | 19 | 1.4 | 3.1 | 0.0 |
| rm | 4096 | workProbe | 1356501 | 0.0 | 0.0 | 0.0 |

Jobs discarded by the re-sync (EDF only tracks jobs; mean per re-sync, max, share of re-syncs that discarded any): rr r16: 0.00, 0, 0%; edf r16: 0.00, 0, 0%; rm r16: 0.00, 0, 0%; rr r4096: 0.00, 0, 0%; edf r4096: 0.00, 0, 0%; rm r4096: 0.00, 0, 0%

Mean gap between consecutive passes of a worker (µs, one value per worker): rr r16: 1.2, 1.2, 1.2, 1.2; edf r16: 1.7, 1.7, 1.9, 2.0; rm r16: 1.1, 1.2, 1.2, 1.2; rr r4096: 0.8, 0.8, 1.0, 1.1; edf r4096: 0.8, 0.8, 1.0, 1.1; rm r4096: 0.8, 0.8, 1.1, 1.3

### What the ratio is

`process_stream_to_message_ratio` is a setting of GR4's scheduler (`core/include/gnuradio-4.0/Scheduler.hpp`,
default 16). Every worker runs a loop of *passes*; in a pass it selects blocks and calls their `work()`. The
worker keeps a pass counter, and once every `ratio` passes it opens a *message phase* before doing any work.
The message phase is where everything that is not stream processing happens, in this order: the scheduler's and
graph's own messages (worker 0 only), cleanup of removed blocks, zombie reaping, adoption of new blocks, the
re-sync of every block's scheduling state (`stateSync`), the static re-ordering of the worker's block list,
each block's `processScheduledMessages()` (settings changes and other block messages), and buffer house-keeping
unless the house-keeping policy is Light. Worker 0 also opens the phase early when a scheduler message is
pending, so the ratio does not bound the latency of scheduler and graph messages; it does bound everything
else on that list. The ratio is a count of passes, not a time: the interval between two message phases is
`ratio` × the worker's pass duration, and the pass duration depends on the policy and the load (the table
below gives both for this run). A power of two is slightly cheaper (the counter wraps with a mask instead of
a modulo).

The re-sync is what makes the ratio matter to EDF. `syncSchedStates()` assigns whole scheduling states, and
under a policy that tracks releases the worker then rebuilds its release storage — on every message phase.
Until 2026-09-22 (`9126f2a`, `7af5453`) that rebuild flattened the whole graph and recomputed its adjacency
each time, and discarded every outstanding job and last-release time, whether or not the graph had changed.
Now the flattened graph and adjacency are cached per worker and rebuilt only when the scheduler's topology
generation has moved (it is bumped whenever work quiescence is released, which every structural change
takes), and the admitted jobs, last-release times and overrun counts are carried across a re-sync whose
block list is pointer-identical and whose generation is unchanged. The re-sync still runs on every message
phase; it is cheaper when nothing changed. Under RR and RM the re-sync is a few microseconds; under EDF it
is the storage rebuild. At the default ratio of 16 that recurs every 16 passes, which is the `stateSync` row
above (since `9126f2a` the marker covers the state rebuild alone; before, it ran to the end of the phase
and read as nearly all of it). At 4096 the same work recurs 256× less often and its busy share falls below
one percent, which is why the sweeps compare policies at 4096.

### Choosing and setting a value

The ratio trades scheduler overhead against reaction time. A larger ratio makes every message phase rarer:
less re-sync cost, more passes per second, but a longer wait before a settings change is applied, a new block
is adopted or a removed one is released. A smaller ratio does the reverse. The rule is: take the largest
ratio whose message-phase interval is still inside the latency the application can tolerate for a settings
change or a graph mutation, and check that `stateSync`'s busy share is negligible at that ratio. For a
static graph with no settings traffic while running, which is what the sweeps use, 4096 is that value on this
machine (an interval in the tens of milliseconds, re-sync below 1 %). A graph that expects settings messages
at a given rate needs an interval shorter than their spacing; divide that by the measured pass duration to
get the ratio, and round to a power of two. Do not pick the ratio to make one policy look better: it changes
the re-sync cost of EDF and the pass rate of every policy at once, so policies are compared at the same
ratio.

Setting it: `rx_latency4 --sched-ratio N` stages it as the scheduler setting `process_stream_to_message_ratio`
in the property map applied before the scheduler starts (staging then applying; `set()` alone changes
nothing); the sweep driver takes `sched_ratio` from the machine profile (`experiments/profiles/*.json`,
`x86-8core-r4096.json` is the 4096 variant) and passes it the same way; in GR4 code it is the constructor
setting `{"process_stream_to_message_ratio", gr::Size_t(N)}` of any `gr::scheduler::Simple`. The value each
run used is recorded as `sched_ratio` in its `latency_summary.json` and `trace_meta.json`.

| policy | ratio | passes per s | mean pass µs | message phases per s | interval between message phases ms | stateSync busy % |
|---|---|---|---|---|---|---|
| rr | 16 | 90214 | 9.9 | 5638 | 0.18 | 2.6 |
| edf | 16 | 156967 | 4.5 | 9811 | 0.10 | 10.8 |
| rm | 16 | 78204 | 11.6 | 4888 | 0.20 | 2.3 |
| rr | 4096 | 93124 | 9.8 | 23 | 44.08 | 0.1 |
| edf | 4096 | 208933 | 3.9 | 51 | 19.62 | 0.1 |
| rm | 4096 | 79815 | 11.5 | 19 | 51.39 | 0.0 |

## How this run reads against the earlier ones

*Hand-written after the generated part; a regeneration of this file drops it. Same KVM/QEMU guest (8 vCPUs, no
frequency control), same runbook, same stimulus cell and pass budget as the three earlier M6 runs on this machine:
the published one (`sweep-x86-8core/micro`, scheduler before the re-sync work), 2026-09-22 (same scheduler, a
fresh baseline), and 2026-09-22b (after the per-worker topology cache and job carry, `d3c74e1`). This run is at
`df99fb1`, which adds `a1be12a` (the successor cache keyed on the worker's own list and owning nothing) and
`fa7e6d7` (a thread's trace ring built before any span can contain it). Every figure below is a single 20 s run
per cell, as M6 is defined.*

**The re-sync fix holds, and this run is its cleanest measurement.** At the default ratio of 16, EDF's message
phase is 13.8 µs mean and 13.6 % of a worker, against 47 µs and 29–30 % in both runs before the fix and 15.4 µs
and 14.3 % in the run right after it. The 5 Msps receiver's late share under EDF at ratio 16 is 4.5 %, from
45–47 % before the fix and 6.5 % right after. The cost that made EDF lose every earlier comparison at the default
ratio is gone; what remains of it — the release-storage rebuild the narrowed `stateSync` marker now measures alone
— is about 11 µs and 11 % of an EDF worker at ratio 16, about two and a half times RR's or RM's re-sync, and it is
still the largest single item in EDF's phase. `a1be12a` changed how the cache is keyed and what it owns, not how
often it is rebuilt on a graph that never mutates, so the small further drop from 22b (phase −10 %, `stateSync`
12.5 → 11.0 µs) is within what one repeat can resolve and should not be read as its effect.

**Ratio 4096 now reads as it did before the fix, which settles yesterday's open question.** In run 22b every
policy, RR and RM included, came out worse at 4096 than in the two runs before it, and the comparison said that pair
could not be interpreted. This run's 4096 numbers sit back on the earlier ones: the 5 Msps receiver's mean batch
response time is 166 / 133 / 109 µs under RR / EDF / RM (published 152 / 133 / 104; 22: 151 / 127 / 115; 22b:
185 / 170 / 126) and its late share under EDF is 4.1 % (published 4.6, 22: 3.6, 22b: 15.7). The 22b excursion was
machine noise on a single repeat, not the change, and nothing in the scheduler work moved the 4096 point, which is
what was expected: at 4096 the phase recurs 20–50 times a second and costs a fraction of a percent whichever way it
is built.

**The message-phase means at 4096 are now trustworthy; the earlier ones were not.** Before `fa7e6d7`, a worker's
first message phase absorbed the construction of its trace ring, about a millisecond. At ratio 16 that one phase
was lost among thousands; at 4096 a worker sees only 35–170 phases in the window, so one outlier moved the mean by
tens of microseconds and set the p99. That is why the 4096 phase means in the earlier runs were 30–95 µs with p99s
of 120–250 µs, wandering between runs, while at 16 they were steady. This run's 4096 phase means are 28–34 µs for
all three policies with an EDF p99 of 49 µs, and its `stateSync` at 4096 is 21–25 µs for all three, where the
earlier runs had EDF at 54–93 µs. The earlier 4096 phase and `stateSync` rows should be treated as contaminated by
that one-off, not as evidence of anything about the policies; the ratio-16 rows of the earlier runs are unaffected.

**What is unchanged.** RR and RM are where they have been in all four runs: phase 7.3–7.4 µs and 3.6–4.1 % of a
worker at ratio 16, the 5 Msps receiver about 20 % late under RR and about 1 % under RM. The re-sync discards no
jobs in any cell. And the reason to raise the ratio for a static graph still stands — at 16 EDF spends 13.6 % of
each worker in message phases against 4 % for RR and RM, and its pass rate is a third higher at 4096 than at 16 —
but it is a smaller reason than the one the published run found, and the default no longer makes EDF the worst
policy on this workload.
