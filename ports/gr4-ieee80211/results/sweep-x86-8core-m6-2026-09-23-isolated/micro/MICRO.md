# Microbenchmarks
Generated by `scripts/rt-microbench4`; raw values in `micro.json`.

## M6 — scheduler pass cost on four workers (light mix 1.25/1.25/2.5/5 Msps, N = 1024, rotation 1, 20 s, every category traced, selection pass bounded at 51 µs)

Batch response time per receiver (mean µs, and the fraction of batches later than the receiver's own period):

| policy | ratio | rx 0 | rx 1 | rx 2 | rx 3 (5 Msps) | EDF pipeline misses |
|---|---|---|---|---|---|---|
| rr | 16 | 134 (1.4%) | 155 (1.2%) | 188 (2.1%) | 197 (13.8%) | - |
| edf | 16 | 141 (0.0%) | 128 (0.0%) | 147 (0.0%) | 120 (1.7%) | 0 |
| rm | 16 | 242 (2.7%) | 313 (3.0%) | 210 (2.7%) | 146 (3.9%) | - |
| rr | 4096 | 145 (0.7%) | 151 (0.7%) | 165 (1.4%) | 177 (15.9%) | - |
| edf | 4096 | 185 (1.7%) | 221 (1.2%) | 153 (1.8%) | 140 (3.9%) | 104 |
| rm | 4096 | 220 (2.1%) | 225 (2.2%) | 160 (1.9%) | 130 (2.6%) | - |

Scheduler events per worker inside the window (rate, mean and p99 duration, share of the worker's time):

| policy | ratio | event | per s | mean µs | p99 µs | busy % |
|---|---|---|---|---|---|---|
| rr | 16 | sweep | 100860 | 9.4 | 61.4 | 95.1 |
| rr | 16 | messagePhase | 6304 | 6.5 | 14.6 | 4.1 |
| rr | 16 | stateSync | 6304 | 4.5 | 11.8 | 2.9 |
| rr | 16 | schedulerMessages | 1675 | 0.1 | 0.1 | 0.0 |
| rr | 16 | removalCleanup | 6304 | 0.0 | 0.1 | 0.0 |
| rr | 16 | blockMessages | 6304 | 0.2 | 0.5 | 0.2 |
| rr | 16 | workProbe | 1659633 | 0.0 | 0.0 | 0.0 |
| edf | 16 | sweep | 314653 | 2.7 | 33.5 | 84.5 |
| edf | 16 | messagePhase | 19666 | 6.6 | 13.9 | 13.1 |
| edf | 16 | stateSync | 19666 | 5.0 | 10.9 | 9.9 |
| edf | 16 | schedulerMessages | 6606 | 0.1 | 0.1 | 0.0 |
| edf | 16 | removalCleanup | 19666 | 0.0 | 0.1 | 0.1 |
| edf | 16 | blockMessages | 19666 | 0.2 | 0.4 | 0.3 |
| edf | 16 | releaseScan | 548507 | 0.5 | 1.0 | 26.6 |
| edf | 16 | workProbe | 190022 | 0.0 | 0.0 | 0.0 |
| rm | 16 | sweep | 83401 | 11.5 | 100.3 | 95.7 |
| rm | 16 | messagePhase | 5213 | 6.9 | 15.9 | 3.6 |
| rm | 16 | stateSync | 5213 | 4.8 | 12.9 | 2.5 |
| rm | 16 | schedulerMessages | 1434 | 0.1 | 0.2 | 0.0 |
| rm | 16 | removalCleanup | 5213 | 0.0 | 0.1 | 0.0 |
| rm | 16 | blockMessages | 5212 | 0.3 | 0.7 | 0.1 |
| rm | 16 | workProbe | 1409726 | 0.0 | 0.0 | 0.0 |
| rr | 4096 | sweep | 106851 | 9.3 | 64.3 | 99.1 |
| rr | 4096 | messagePhase | 26 | 24.7 | 43.9 | 0.1 |
| rr | 4096 | stateSync | 26 | 18.1 | 30.3 | 0.0 |
| rr | 4096 | schedulerMessages | 7 | 0.7 | 1.4 | 0.0 |
| rr | 4096 | removalCleanup | 26 | 0.1 | 0.1 | 0.0 |
| rr | 4096 | blockMessages | 26 | 1.1 | 2.5 | 0.0 |
| rr | 4096 | workProbe | 1770378 | 0.0 | 0.0 | 0.0 |
| edf | 4096 | sweep | 408042 | 2.4 | 32.7 | 96.7 |
| edf | 4096 | messagePhase | 100 | 14.4 | 32.7 | 0.1 |
| edf | 4096 | stateSync | 100 | 10.4 | 25.5 | 0.1 |
| edf | 4096 | schedulerMessages | 36 | 0.2 | 0.5 | 0.0 |
| edf | 4096 | removalCleanup | 100 | 0.0 | 0.1 | 0.0 |
| edf | 4096 | blockMessages | 100 | 0.8 | 1.2 | 0.0 |
| edf | 4096 | releaseScan | 668161 | 0.5 | 1.0 | 35.0 |
| edf | 4096 | workProbe | 216329 | 0.0 | 0.0 | 0.0 |
| rm | 4096 | sweep | 91430 | 10.9 | 90.8 | 99.2 |
| rm | 4096 | messagePhase | 22 | 25.6 | 45.8 | 0.1 |
| rm | 4096 | stateSync | 22 | 18.3 | 30.2 | 0.0 |
| rm | 4096 | schedulerMessages | 6 | 0.6 | 1.7 | 0.0 |
| rm | 4096 | removalCleanup | 22 | 0.1 | 0.1 | 0.0 |
| rm | 4096 | blockMessages | 22 | 1.2 | 3.2 | 0.0 |
| rm | 4096 | workProbe | 1553943 | 0.0 | 0.0 | 0.0 |

Jobs discarded by the re-sync (EDF only tracks jobs; mean per re-sync, max, share of re-syncs that discarded any): rr r16: 0.00, 0, 0%; edf r16: 0.00, 0, 0%; rm r16: 0.00, 0, 0%; rr r4096: 0.00, 0, 0%; edf r4096: 0.00, 0, 0%; rm r4096: 0.00, 0, 0%

Mean gap between consecutive passes of a worker (µs, one value per worker): rr r16: 0.5, 0.5, 0.5, 0.5; edf r16: 0.5, 0.5, 0.5, 0.5; rm r16: 0.5, 0.6, 0.5, 0.5; rr r4096: 0.1, 0.1, 0.1, 0.1; edf r4096: 0.1, 0.1, 0.1, 0.1; rm r4096: 0.1, 0.1, 0.1, 0.1

### What the ratio is

`process_stream_to_message_ratio` is a setting of GR4's scheduler (`core/include/gnuradio-4.0/Scheduler.hpp`,
default 16). Every worker runs a loop of *passes*; in a pass it selects blocks and calls their `work()`. The
worker keeps a pass counter, and once every `ratio` passes it opens a *message phase* before doing any work.
The message phase is where everything that is not stream processing happens, in this order: the scheduler's and
graph's own messages (worker 0 only), cleanup of removed blocks, zombie reaping, adoption of new blocks, the
re-sync of every block's scheduling state (`stateSync`), the static re-ordering of the worker's block list,
each block's `processScheduledMessages()` (settings changes and other block messages), and buffer house-keeping
unless the house-keeping policy is Light. Worker 0 also opens the phase early when a scheduler message is
pending, so the ratio does not bound the latency of scheduler and graph messages; it does bound everything
else on that list. The ratio is a count of passes, not a time: the interval between two message phases is
`ratio` × the worker's pass duration, and the pass duration depends on the policy and the load (the table
below gives both for this run). A power of two is slightly cheaper (the counter wraps with a mask instead of
a modulo).

The re-sync is what makes the ratio matter to EDF. `syncSchedStates()` assigns whole scheduling states, and
under a policy that tracks releases the worker then rebuilds its release storage — on every message phase.
Until 2026-09-22 (`9126f2a`, `7af5453`) that rebuild flattened the whole graph and recomputed its adjacency
each time, and discarded every outstanding job and last-release time, whether or not the graph had changed.
Now the flattened graph and adjacency are cached per worker and rebuilt only when the scheduler's topology
generation has moved (it is bumped whenever work quiescence is released, which every structural change
takes), and the admitted jobs, last-release times and overrun counts are carried across a re-sync whose
block list is pointer-identical and whose generation is unchanged. The re-sync still runs on every message
phase; it is cheaper when nothing changed. Under RR and RM the re-sync is a few microseconds; under EDF it
is the storage rebuild. At the default ratio of 16 that recurs every 16 passes, which is the `stateSync` row
above (since `9126f2a` the marker covers the state rebuild alone; before, it ran to the end of the phase
and read as nearly all of it). At 4096 the same work recurs 256× less often and its busy share falls below
one percent, which is why the sweeps compare policies at 4096.

### Choosing and setting a value

The ratio trades scheduler overhead against reaction time. A larger ratio makes every message phase rarer:
less re-sync cost, more passes per second, but a longer wait before a settings change is applied, a new block
is adopted or a removed one is released. A smaller ratio does the reverse. The rule is: take the largest
ratio whose message-phase interval is still inside the latency the application can tolerate for a settings
change or a graph mutation, and check that `stateSync`'s busy share is negligible at that ratio. For a
static graph with no settings traffic while running, which is what the sweeps use, 4096 is that value on this
machine (an interval in the tens of milliseconds, re-sync below 1 %). A graph that expects settings messages
at a given rate needs an interval shorter than their spacing; divide that by the measured pass duration to
get the ratio, and round to a power of two. Do not pick the ratio to make one policy look better: it changes
the re-sync cost of EDF and the pass rate of every policy at once, so policies are compared at the same
ratio.

Setting it: `rx_latency4 --sched-ratio N` stages it as the scheduler setting `process_stream_to_message_ratio`
in the property map applied before the scheduler starts (staging then applying; `set()` alone changes
nothing); the sweep driver takes `sched_ratio` from the machine profile (`experiments/profiles/*.json`,
`x86-8core-r4096.json` is the 4096 variant) and passes it the same way; in GR4 code it is the constructor
setting `{"process_stream_to_message_ratio", gr::Size_t(N)}` of any `gr::scheduler::Simple`. The value each
run used is recorded as `sched_ratio` in its `latency_summary.json` and `trace_meta.json`.

| policy | ratio | passes per s | mean pass µs | message phases per s | interval between message phases ms | stateSync busy % |
|---|---|---|---|---|---|---|
| rr | 16 | 100860 | 9.4 | 6304 | 0.16 | 2.9 |
| edf | 16 | 314653 | 2.7 | 19666 | 0.05 | 9.9 |
| rm | 16 | 83401 | 11.5 | 5213 | 0.19 | 2.5 |
| rr | 4096 | 106851 | 9.3 | 26 | 38.53 | 0.0 |
| edf | 4096 | 408042 | 2.4 | 100 | 10.03 | 0.1 |
| rm | 4096 | 91430 | 10.9 | 22 | 44.90 | 0.0 |

## How this run reads

*Hand-written after the generated part; a regeneration of this file drops it.
Read per `docs/m6-runbook.md` §8–9: a new baseline, pass period first,
`stateSync` second, busy shares never subtracted. Compared where it helps
against the morning run of the same day (`../../sweep-x86-8core-m6-2026-09-23/micro/`),
which is the last run before both of the changes below. One 20 s run per cell,
as M6 is defined.*

**Two things changed at once, so nothing here attributes a number to one of
them.** (1) The scheduler: the tree is at `ca7e154`, with the per-pass fixes
`ea42d87`, `1e898ad`, `3eb2f59`, `d09a1e7` and `8fb0ed9` merged since the
morning run at `df99fb1`. (2) **The guest** — this is the first M6 on the
changed platform, and the differentiator from every earlier run: the same
KVM/QEMU guest (8 vCPUs, "QEMU Virtual CPU version 2.5+", no frequency
control) now runs an RT kernel, `7.0.11-rt` (`PREEMPT_RT`), instead of
`7.0.0-31-generic`; CPUs 4–7 are isolated (`isolcpus=domain,managed_irq,4-7
nohz_full=4-7 rcu_nocbs=4-7 irqaffinity=0-3`), every other process is confined
to 0–3, and the driver passed `--cpus 4-7`, so the four workers ran one per
isolated CPU under `SCHED_OTHER` (`worker_cpus` in every `latency_summary.json`;
no real-time priority, `rt_prio` 0). Every earlier M6 shared all eight vCPUs
between the workers, the driver, the throttle timer threads, the trace export
and the guest's housekeeping. `docs/e2e-sweep-runbook.md` §13 describes the
setup and its verification.

**The pass period, first.** Under EDF a pass now takes half what it did:
3.18 µs at ratio 16 against 6.37 (2.0×), 2.45 against 4.79 at 4096 (1.95×).
Round robin's period shortened by 11 % (11.08 → 9.91 µs) and rate monotonic's
by 6 % (12.79 → 11.99). The runbook predicted the direction and, for EDF, the
size (its profiling machine showed 6.0 → 3.0 µs). The mean gap between passes
fell from 1.2–2.0 µs to 0.46–0.53 µs per worker at ratio 16 and to 0.08 µs at
4096; at ratio 16 the amortised message phase alone accounts for 0.41 µs of
that gap under both RR and EDF (6.5 and 6.6 µs per phase, every sixteenth
pass), so the residual no marker covers is now about 0.1 µs per pass, from
about 1 µs before. `removalCleanup`, the per-pass cost `ea42d87` removed from
the pass and left only in the phase, reads 0.03–0.05 µs per phase against
0.7–0.9 before.

**`stateSync`, second.** EDF's re-sync is 5.0 µs mean at ratio 16, from 11.0
(2.2× cheaper), and 1.1× round robin's 4.5 µs — the release-storage rebuild
that remained after the topology cache is now close to the cost of assigning
the states. Its busy share, 9.9 %, fell less than its cost, because the phase
recurs twice as often (19 666 per second per worker against 9 811): the
amplifier of §9. For the same reason EDF's message-phase share is unchanged
(13.1 % against 13.6 %) while the phase itself halved (6.6 µs against 13.8),
and the release scan's share rose (26.6 % against 21.6 %) while each scan got
cheaper (0.48 µs against 0.68). None of those shares is a regression.

**Batch response time.** At ratio 16 EDF is now the best policy on every
receiver of this workload — 141 / 128 / 147 / 120 µs, the 5 Msps receiver
1.7 % late, no pipeline miss — where in the morning it was 164 / 189 / 148 /
136 with 4.5 % late; the ratio-16 handicap that M6 was built to expose is
gone. Round robin at ratio 16 is where it was on the fast receiver (197 µs,
13.8 % late) and rate monotonic is worse on every receiver (242 / 313 / 210 /
146 against 158 / 216 / 150 / 119). Two things in this table cannot be read
from one run each and are left as observations: every policy now shows a
late share of 1–3 % on the slow receivers, where the morning run and every
earlier one read 0.0–0.6 %; and EDF at 4096 recorded 104 pipeline deadline
misses against 5 in the morning and 0–3 in every earlier run, with its batch
response times otherwise unremarkable (185 / 221 / 153 / 140). Whether those
belong to the RT kernel, to the pinning, or to the scheduler changes needs
repeats with one factor varied at a time; a 20 s single run cannot say.

**Windows.** Faster passes mean more records per second, so the rings held
less: 1.6–1.8 s of an RR or RM run and 2.4–2.7 s of an EDF run, against
1.8–1.9 and 3.4–4.1 in the morning. Every figure above is over that window.
