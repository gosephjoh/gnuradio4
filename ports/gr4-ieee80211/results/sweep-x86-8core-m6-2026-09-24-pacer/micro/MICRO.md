# Microbenchmarks
Generated by `scripts/rt-microbench4`; raw values in `micro.json`.

## M6 — scheduler pass cost on four workers (light mix 1.25/1.25/2.5/5 Msps x 2.331, N = 1024, rotation 1, 20.0 s, input: pacer, every category traced, selection pass bounded at 22 µs)

Calibration (rr at ratio 4096, 10 s steps, band 0.93-0.95 for the busiest worker's demand): x1.000 -> 0.395; x2.379 -> 0.939 (saturated); x1.690 -> 0.667; x2.034 -> 0.8; x2.207 -> 0.874; x2.373 -> 0.928 (saturated); x2.290 -> 0.904; x2.331 -> 0.918. Scale used: 2.331275671054259 (band not reached: the largest scale that kept up; the reference saturated at 2.3728022557597996)

In the workload's units, the same under every policy (docs/m6-runbook.md 9): the busiest worker's demand, productive work() calls per second per worker, the end-to-end latency from the pacer's write to the sink (mean / p99 µs, fastest receiver), the entry block's wait from write to first run (mean / p99 µs, fastest receiver), and whether the run kept up:

| policy | ratio | demand max | productive calls/s per worker | E2E rx3 mean / p99 | entry wait rx3 mean / p99 | saturated |
|---|---|---|---|---|---|---|
| rr | 16 | 0.914 | 86274, 89844, 87339, 90058 | 1088.6 / 1522.2 | 19.3 / 109.2 | no |
| edf | 16 | 0.921 | 87460, 89826, 93088, 93551 | 4467.6 / 6742.8 | 1376.5 / 1613.6 | yes |
| rm | 16 | 0.840 | 76728, 80684, 77402, 79801 | 485.7 / 753.8 | 12.0 / 69.2 | yes |
| rr | 4096 | 0.916 | 86333, 89939, 87536, 90242 | 1046.0 / 3392.2 | 18.1 / 106.4 | no |
| edf | 4096 | 0.917 | 87267, 90533, 92838, 92637 | 1276.0 / 4778.0 | 12.1 / 72.6 | yes |
| rm | 4096 | 0.889 | 83244, 87279, 85069, 85957 | 477.5 / 708.9 | 11.7 / 68.8 | yes |

Batch response time per receiver (mean µs, and the fraction of batches later than the receiver's own period):

| policy | ratio | rx 0 | rx 1 | rx 2 | rx 3 (5 Msps) | EDF pipeline misses |
|---|---|---|---|---|---|---|
| rr | 16 | 283 (15.2%) | 311 (28.5%) | 354 (99.4%) | 540 (100.0%) | - |
| edf | 16 | 1537 (100.0%) | 2398 (100.0%) | 2273 (100.0%) | 182406 (100.0%) | 437515 |
| rm | 16 | 827 (98.8%) | 1319476 (100.0%) | 585 (100.0%) | 235 (99.9%) | - |
| rr | 4096 | 273 (11.9%) | 299 (22.7%) | 343 (99.1%) | 507 (100.0%) | - |
| edf | 4096 | 871 (99.9%) | 1200 (100.0%) | 644 (100.0%) | 285 (100.0%) | 111039 |
| rm | 4096 | 801 (98.9%) | 1235735 (100.0%) | 573 (100.0%) | 230 (100.0%) | - |

Scheduler events per worker inside the window (rate, mean and p99 duration, share of the worker's time):

| policy | ratio | event | per s | mean µs | p99 µs | busy % |
|---|---|---|---|---|---|---|
| rr | 16 | sweep | 49164 | 19.9 | 139.2 | 97.8 |
| rr | 16 | messagePhase | 3073 | 6.3 | 14.5 | 1.9 |
| rr | 16 | stateSync | 3073 | 4.3 | 11.7 | 1.3 |
| rr | 16 | schedulerMessages | 618 | 0.1 | 0.2 | 0.0 |
| rr | 16 | removalCleanup | 3073 | 0.0 | 0.1 | 0.0 |
| rr | 16 | blockMessages | 3073 | 0.3 | 0.5 | 0.1 |
| rr | 16 | workProbe | 653701 | 0.0 | 0.0 | 0.0 |
| edf | 16 | sweep | 127184 | 7.4 | 71.4 | 94.2 |
| edf | 16 | messagePhase | 7949 | 6.4 | 13.6 | 5.1 |
| edf | 16 | stateSync | 7949 | 4.8 | 10.8 | 3.8 |
| edf | 16 | schedulerMessages | 1315 | 0.1 | 0.2 | 0.0 |
| edf | 16 | removalCleanup | 7949 | 0.0 | 0.1 | 0.0 |
| edf | 16 | blockMessages | 7949 | 0.2 | 0.5 | 0.1 |
| edf | 16 | releaseScan | 292096 | 0.3 | 1.1 | 9.8 |
| edf | 16 | workProbe | 73828 | 0.0 | 0.0 | 0.0 |
| rm | 16 | sweep | 97221 | 9.9 | 236.4 | 96.4 |
| rm | 16 | messagePhase | 6076 | 5.1 | 11.9 | 3.1 |
| rm | 16 | stateSync | 6076 | 3.7 | 9.1 | 2.3 |
| rm | 16 | schedulerMessages | 233 | 0.1 | 0.2 | 0.0 |
| rm | 16 | removalCleanup | 6076 | 0.0 | 0.1 | 0.0 |
| rm | 16 | blockMessages | 6076 | 0.1 | 0.4 | 0.1 |
| rm | 16 | workProbe | 589862 | 0.0 | 0.0 | 0.0 |
| rr | 4096 | sweep | 54744 | 18.2 | 135.6 | 99.7 |
| rr | 4096 | messagePhase | 13 | 31.2 | 84.2 | 0.0 |
| rr | 4096 | stateSync | 13 | 17.2 | 27.3 | 0.0 |
| rr | 4096 | schedulerMessages | 3 | 0.8 | 1.4 | 0.0 |
| rr | 4096 | removalCleanup | 13 | 0.0 | 0.1 | 0.0 |
| rr | 4096 | blockMessages | 13 | 1.3 | 3.7 | 0.0 |
| rr | 4096 | workProbe | 737230 | 0.0 | 0.0 | 0.0 |
| edf | 4096 | sweep | 217606 | 4.5 | 64.5 | 98.7 |
| edf | 4096 | messagePhase | 53 | 15.4 | 49.3 | 0.1 |
| edf | 4096 | stateSync | 53 | 10.2 | 27.1 | 0.1 |
| edf | 4096 | schedulerMessages | 9 | 0.4 | 1.2 | 0.0 |
| edf | 4096 | removalCleanup | 53 | 0.0 | 0.1 | 0.0 |
| edf | 4096 | blockMessages | 53 | 0.9 | 2.2 | 0.0 |
| edf | 4096 | releaseScan | 308450 | 0.5 | 1.1 | 15.4 |
| edf | 4096 | workProbe | 24 | 0.0 | 0.0 | 0.0 |
| rm | 4096 | sweep | 31521 | 31.6 | 736.9 | 99.8 |
| rm | 4096 | messagePhase | 8 | 52.4 | 483.2 | 0.0 |
| rm | 4096 | stateSync | 8 | 20.6 | 31.1 | 0.0 |
| rm | 4096 | schedulerMessages | 1 | 1.2 | 1.8 | 0.0 |
| rm | 4096 | removalCleanup | 8 | 0.0 | 0.1 | 0.0 |
| rm | 4096 | blockMessages | 8 | 1.7 | 4.2 | 0.0 |
| rm | 4096 | workProbe | 471692 | 0.0 | 0.0 | 0.0 |

Jobs discarded by the re-sync (EDF only tracks jobs; mean per re-sync, max, share of re-syncs that discarded any): rr r16: 0.00, 0, 0%; edf r16: 0.00, 0, 0%; rm r16: 0.00, 0, 0%; rr r4096: 0.00, 0, 0%; edf r4096: 0.00, 0, 0%; rm r4096: 0.00, 0, 0%

Mean gap between consecutive passes of a worker (µs, one value per worker): rr r16: 0.5, 0.4, 0.5, 0.4; edf r16: 0.5, 0.4, 0.6, 0.4; rm r16: 0.5, 0.4, 0.4, 0.3; rr r4096: 0.1, 0.1, 0.1, 0.1; edf r4096: 0.1, 0.1, 0.1, 0.1; rm r4096: 0.1, 0.1, 0.6, 0.1

Passes ended by a bound (per s per worker, time budget / selection count; round robin has no bound): rr r16: 0.0 / 0.0; edf r16: 15136.3 / 0.0; rm r16: 0.0 / 437.7; rr r4096: 0.0 / 0.0; edf r4096: 14038.7 / 0.0; rm r4096: 0.0 / 518.5

### What the ratio is

`process_stream_to_message_ratio` is a setting of GR4's scheduler (`core/include/gnuradio-4.0/Scheduler.hpp`,
default 16). Every worker runs a loop of *passes*; in a pass it selects blocks and calls their `work()`. The
worker keeps a pass counter, and once every `ratio` passes it opens a *message phase* before doing any work.
The message phase is where everything that is not stream processing happens, in this order: the scheduler's and
graph's own messages (worker 0 only), cleanup of removed blocks, zombie reaping, adoption of new blocks, the
re-sync of every block's scheduling state (`stateSync`), the static re-ordering of the worker's block list,
each block's `processScheduledMessages()` (settings changes and other block messages), and buffer house-keeping
unless the house-keeping policy is Light. Worker 0 also opens the phase early when a scheduler message is
pending, so the ratio does not bound the latency of scheduler and graph messages; it does bound everything
else on that list. The ratio is a count of passes, not a time: the interval between two message phases is
`ratio` × the worker's pass duration, and the pass duration depends on the policy and the load (the table
below gives both for this run). A power of two is slightly cheaper (the counter wraps with a mask instead of
a modulo).

The re-sync is what makes the ratio matter to EDF. `syncSchedStates()` assigns whole scheduling states, and
under a policy that tracks releases the worker then rebuilds its release storage — on every message phase.
Until 2026-09-22 (`9126f2a`, `7af5453`) that rebuild flattened the whole graph and recomputed its adjacency
each time, and discarded every outstanding job and last-release time, whether or not the graph had changed.
Now the flattened graph and adjacency are cached per worker and rebuilt only when the scheduler's topology
generation has moved (it is bumped whenever work quiescence is released, which every structural change
takes), and the admitted jobs, last-release times and overrun counts are carried across a re-sync whose
block list is pointer-identical and whose generation is unchanged. The re-sync still runs on every message
phase; it is cheaper when nothing changed. Under RR and RM the re-sync is a few microseconds; under EDF it
is the storage rebuild. At the default ratio of 16 that recurs every 16 passes, which is the `stateSync` row
above (since `9126f2a` the marker covers the state rebuild alone; before, it ran to the end of the phase
and read as nearly all of it). At 4096 the same work recurs 256× less often and its busy share falls below
one percent, which is why the sweeps compare policies at 4096.

### Choosing and setting a value

The ratio trades scheduler overhead against reaction time. A larger ratio makes every message phase rarer:
less re-sync cost, more passes per second, but a longer wait before a settings change is applied, a new block
is adopted or a removed one is released. A smaller ratio does the reverse. The rule is: take the largest
ratio whose message-phase interval is still inside the latency the application can tolerate for a settings
change or a graph mutation, and check that `stateSync`'s busy share is negligible at that ratio. For a
static graph with no settings traffic while running, which is what the sweeps use, 4096 is that value on this
machine (an interval in the tens of milliseconds, re-sync below 1 %). A graph that expects settings messages
at a given rate needs an interval shorter than their spacing; divide that by the measured pass duration to
get the ratio, and round to a power of two. Do not pick the ratio to make one policy look better: it changes
the re-sync cost of EDF and the pass rate of every policy at once, so policies are compared at the same
ratio.

Setting it: `rx_latency4 --sched-ratio N` stages it as the scheduler setting `process_stream_to_message_ratio`
in the property map applied before the scheduler starts (staging then applying; `set()` alone changes
nothing); the sweep driver takes `sched_ratio` from the machine profile (`experiments/profiles/*.json`,
`x86-8core-r4096.json` is the 4096 variant) and passes it the same way; in GR4 code it is the constructor
setting `{"process_stream_to_message_ratio", gr::Size_t(N)}` of any `gr::scheduler::Simple`. The value each
run used is recorded as `sched_ratio` in its `latency_summary.json` and `trace_meta.json`.

| policy | ratio | passes per s | mean pass µs | message phases per s | interval between message phases ms | stateSync busy % |
|---|---|---|---|---|---|---|
| rr | 16 | 49164 | 19.9 | 3073 | 0.33 | 1.3 |
| edf | 16 | 127184 | 7.4 | 7949 | 0.13 | 3.8 |
| rm | 16 | 97221 | 9.9 | 6076 | 0.16 | 2.3 |
| rr | 4096 | 54744 | 18.2 | 13 | 75.05 | 0.0 |
| edf | 4096 | 217606 | 4.5 | 53 | 18.83 | 0.1 |
| rm | 4096 | 31521 | 31.6 | 8 | 126.25 | 0.0 |

## How this run reads

*Hand-written after the generated part; a regeneration of this file drops it.
Read per `docs/m6-runbook.md` §5–§9. Tree at `a15966c`; same KVM guest,
RT kernel `7.0.11-rt`, CPUs 4–7 isolated, workers pinned one per isolated
CPU under `SCHED_OTHER`, the pacer on CPU 3. One 20 s run per cell.*

**Not comparable with any earlier M6.** This is the first run on the pacer
(`--feed pacer`): the input is written into each receiver's entry block by a
thread outside the scheduler at the chunk's nominal instant, so the file
source, throttle and arrival stamper are gone (16 blocks per receiver instead
of 18, dealt differently over the workers), EDF's blocks declare periods of 0
instead of 1 µs, and the rates are **calibrated to near capacity** rather than
fixed at the light mix: every earlier run was at scale 1, this one at 2.331×
(2.91 / 2.91 / 5.83 / 11.66 Msps; the fastest receiver's chunk period 87.8 µs;
pass budget 22 µs). Every batch response time and late share in the tables
above is measured against a period a quarter the length of the earlier runs',
at two and a third times the input, on a different graph; the late shares of
~100 % everywhere are what running at capacity looks like, not a regression,
and none of these rows can be set beside a throttle-mode row. The runbook's
§8 item 6 lists the reasons; §9 gives the like-for-like comparison, which is
what follows.

**Calibration did not reach the band.** Eight 10 s steps of round robin at
ratio 4096: demand 0.395 at scale 1, then 2.379× saturated at 0.939, and the
bisection settled between 2.331× (kept up, demand 0.918) and 2.373×
(saturated, 0.928). The six runs use 2.331×, and `band_reached` is false:
the reference's own overhead does not fit in the 5–7 % the band leaves, as
§5 anticipates for this guest. The "kept up" verdict at 2.331× is the write
lag's p95 under ten chunk periods; that step's fastest receiver still logged
69 109 retries and a write-lag p99 of 19.8 ms, so the operating point is at
the very edge of what round robin carries.

**At this input, only round robin keeps up.** Read the "same under every
policy" table first, as §9 says:

- **Round robin** at both ratios: no receiver saturated, write lag p99 of
  6–9 µs at ratio 16 and 8–31 µs at 4096, demand 0.914 / 0.916 — inside
  ±0.02 of the calibration. Its fastest receiver still had retries (3.2 % of
  its chunks at ratio 16, 5.3 % at 4096: a chunk found the entry buffer full
  and was written late), so it too is at capacity; the entry wait is 18–49 µs
  mean, 69–123 µs p99 across the receivers, and the fastest receiver's
  end-to-end latency is 1.05–1.09 ms mean, 1.5 / 3.4 ms p99.
- **EDF** saturates on the fastest receiver at both ratios: write lag p95
  197 ms at ratio 16 (entry wait 1.38 ms mean on that receiver — a standing
  backlog) and 17 ms at 4096 (entry wait 12 µs mean, 48 µs p99: it nearly
  keeps up, and its fastest receiver's end-to-end p50 of 606 µs is below round
  robin's 995, but its p95–p99 of 4.4–4.8 ms and 199 k retries are the
  backlog surfacing). Demand 0.921 / 0.917, inside the band: EDF is paying
  its scheduling cost, not losing data. Its pass is ended by the 22 µs time
  budget on 12 % of passes at ratio 16 and 6.5 % at 4096.
- **Rate monotonic** saturates on a *slow* receiver, chain 1, at both ratios:
  write lag p95 3.3–3.5 s, end-to-end latency 4.4–5.5 s mean, 4 829 / 5 002
  of its 5 999 frames delivered within the run, entry wait ~1 ms — while its
  fastest receiver is the best served of the whole table (478–486 µs mean,
  709–754 µs p99, entry wait 12 µs). That is the starvation the published
  sweep found at four workers, now at capacity. Its demand, 0.840 / 0.889, is
  outside ±0.02 of the calibration, and §6 asks that this be explained
  before anything is compared: a starved receiver's blocks do no productive
  work, so the busiest worker's demand falls with it. The comparison of §9 —
  the input each policy sustains — is not available from this pass; the
  saturation scan (`--m6-saturation-scan`) is what provides it, and is the
  next run to make.

**The scheduler-cost rows are in each policy's own units** and, per §9, not
subtracted across policies. Pass periods: RR 20.3 / 18.3 µs (ratio 16 /
4096), EDF 7.9 / 4.6, RM 10.3 / 31.7; the gap between passes is 0.4–0.5 µs
at ratio 16 and 0.06–0.08 at 4096 on every worker but one (RM at 4096,
worker 2: 0.56 µs and a sweep p99 of 737 µs — the worker of the starved
receiver's blocks). Round robin's pass is 2.6–4.0× EDF's because a round-robin
pass calls every block and EDF's calls only what is releasable, which is the
same fact as EDF's release scan taking 9.8–15.4 % of a worker and round robin
having none. `stateSync` is 3.7–4.8 µs at ratio 16 under all three, 1.3–3.8 %
of a worker; at 4096 it rests on 8–53 phases inside the window. Under EDF the
re-sync discarded no jobs.

**Windows.** The rings held 1.4–2.5 s of the RR and RM runs and 4.6–7.3 s of
the EDF runs (`window_covered_s`, per run in `batch_rt.json`); the trace-based
rows describe those seconds. The pacer's figures — write lag, saturation,
end-to-end latency, entry wait's numerator — cover the whole 12 s window,
which is why the saturation verdicts above need no trace.
